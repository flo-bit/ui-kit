## Usage

A prebuilt Atmosphere hero animation, perfect for atproto/Bluesky login screens. Both `lottie-web` and the animation JSON are lazy-loaded, so they only download when the component mounts.

```svelte
<AtmosphereAnimation class="max-w-xl" />
```

Set size with regular classes. Defaults to `aspect-video w-full`.
